{
  "name": "@wireguard-ops-cockpit/tmux-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/**/*.test.mjs",
    "coverage": "npm run build && c8 --check-coverage --branches 90 --functions 100 --lines 100 --statements 100 --reporter text-summary --reporter json-summary node --test test/**/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "c8": "^11.0.0",
    "typescript": "^5.8.3"
  }
}
